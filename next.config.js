/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images:{
    domains: ["www.androidguys.com", "upload.wikimedia.org", "islamyearul.xyz", "images.unsplash.com"]
  }
}

module.exports = nextConfig
